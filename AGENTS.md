# 学习教练开发约定

修改界面结构、样式、文案、设置或模型面向用户的输出前，必须阅读 `design-system/MASTER.md`，按其中中文术语、视觉令牌和页面规则实现。新增组件先复用现有样式；确需新增规则时同步更新该文档。

验证使用独立测试库，不读取或复制用户插件配置中的密钥。运行 `npm test`、`npm run build`、`npm run smoke`；界面或交互变更在可运行 Obsidian 的环境补跑 `npm run test:e2e`。明确区分移动模拟和手机真机测试。
