# 安全与隐私

请不要在公开 issue、截图、日志或代码中粘贴访问密钥。

学习教练把服务地址、访问密钥和学习现场保存在当前笔记库的插件数据中。访问密钥未加密保存，由使用者自行管理。插件只会把你选定的笔记片段、学习目标和相关回答发送到你配置的模型服务；不会自动发送整个笔记库。

公开发布时，`data.json` 和 `data.backup.json` 不属于发布内容。它们在项目中被忽略，构建包也只包含 `main.js`、`manifest.json` 和 `styles.css`。如果密钥曾经进入 Git 历史，请立即在服务商后台撤销并重新生成；仅删除文件不足以撤销历史记录中的密钥。

发现代码安全问题时，请通过 GitHub 私下联系仓库维护者，不要先公开粘贴密钥或完整配置文件。
